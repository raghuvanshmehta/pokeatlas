"use client";
import Link from "next/link";
import { motion } from "framer-motion";
import { FiArrowUpRight, FiLayers } from "react-icons/fi";
import { TYPE_COLORS, TYPE_NAMES } from "@/constants";
import { formatName } from "@/lib/utils";
import { PageTransition } from "@/components/page-transition";
export default function TypesPage() { return <PageTransition><main className="container-shell py-12 sm:py-16"><div className="max-w-2xl"><div className="mb-4 inline-flex items-center gap-2 rounded-full bg-[#fffbe1] px-3 py-1.5 text-[11px] font-bold uppercase tracking-[.17em] text-[#a37b00] dark:bg-[#3d381f]"><FiLayers /> The battle language</div><h1 className="page-heading font-display text-5xl font-bold sm:text-6xl">Types, decoded.</h1><p className="mt-4 prose-copy">Every Pokémon has an elemental identity. Explore the strengths, weaknesses and personalities behind all 18 types.</p></div><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{TYPE_NAMES.map((type, index) => { const color = TYPE_COLORS[type]; return <motion.div key={type} initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .025 }} whileHover={{ y: -5 }}><Link href={`/types/${type}`} className="group relative block overflow-hidden rounded-[1.5rem] border border-[var(--line)] bg-[var(--surface-solid)] p-5 shadow-card"><div className="absolute -right-9 -top-9 h-28 w-28 rounded-full opacity-25 blur-xl" style={{ background: color.bg }} /><div className="relative flex items-center justify-between"><span className="inline-flex rounded-full px-4 py-2 text-xs font-bold uppercase tracking-[.16em]" style={{ backgroundColor: color.bg, color: color.text }}>{formatName(type)}</span><span className="grid h-9 w-9 place-items-center rounded-full bg-[var(--bg)] text-[var(--muted)] transition group-hover:bg-[#171d32] group-hover:text-white dark:group-hover:bg-white dark:group-hover:text-[#171d32]"><FiArrowUpRight /></span></div><div className="relative mt-8 flex items-end justify-between"><div><p className="font-display text-3xl font-bold">{String(index + 1).padStart(2, "0")}</p><p className="mt-1 text-xs font-semibold text-[var(--muted)]">Explore this type</p></div><div className="h-14 w-14 rounded-full border-[9px] opacity-60" style={{ borderColor: color.glow }} /></div></Link></motion.div>; })}</div></main></PageTransition>; }
