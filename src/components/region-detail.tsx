import Image from "next/image";
import Link from "next/link";
import { RegionInfo } from "@/types";
import { pokemonArtwork, POKEMON_IDS } from "@/constants";
import { formatName } from "@/lib/utils";
export function RegionDetail({ region }: { region: RegionInfo }) { return <section id={region.name.toLowerCase()} className="scroll-mt-28 overflow-hidden rounded-3xl border border-[var(--line)] bg-[var(--surface-solid)] shadow-card"><div className="grid md:grid-cols-[.8fr_1.2fr]"><div className="relative min-h-[250px] overflow-hidden p-7" style={{ background: `linear-gradient(145deg, ${region.color}, #171d32)` }}><div className="absolute -bottom-20 -right-16 h-72 w-72 rounded-full border-[28px] border-white/10" /><p className="relative text-xs font-bold uppercase tracking-[.18em] text-white/70">{region.generation}</p><h2 className="relative mt-3 font-display text-4xl font-bold text-white">{region.name}</h2><p className="relative mt-3 max-w-xs text-sm leading-6 text-white/70">{region.description}</p><div className="relative mt-6 overflow-hidden rounded-2xl border border-white/15 bg-black/10"><img src="/region-map.svg" alt={`${region.name} region map placeholder`} className="h-28 w-full object-cover opacity-75" /></div></div><div className="p-7"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#e85d68]">First partners</p><h3 className="mt-1 font-display text-2xl font-bold">Choose your starter</h3></div><span className="text-sm font-semibold text-[var(--muted)]">{region.legendaries.length} legendary sightings</span></div><div className="mt-6 grid grid-cols-3 gap-3">{region.starters.map((starter) => <Link href={`/pokemon/${starter}`} key={starter} className="group rounded-2xl bg-[var(--bg)] p-3 text-center"><div className="relative mx-auto h-24 w-24"><Image src={pokemonArtwork(POKEMON_IDS[starter] ?? starter)} alt={formatName(starter)} fill sizes="96px" className="object-contain transition group-hover:scale-110" unoptimized /></div><p className="text-xs font-bold capitalize group-hover:text-[#e85d68]">{formatName(starter)}</p></Link>)}</div><p className="mt-6 text-xs font-bold uppercase tracking-[.18em] text-[var(--muted)]">Legendary Pokémon</p><div className="mt-3 flex flex-wrap gap-x-4 gap-y-2">{region.legendaries.map((legendary) => <Link key={legendary} href={`/pokemon/${legendary}`} className="text-sm font-semibold capitalize text-[var(--muted)] hover:text-[#e85d68]">{formatName(legendary)}</Link>)}</div></div></div></section>; }
